# 硕士毕业论文

编译环境：
```bash
sudo apt install texlive-xetex texlive-lang-chinese texlive-science latexmk、
sudo cp /mnt/c/Windows/Fonts/{simsun,simhei,simkai,msyh*,times*}.tt[fc] /usr/local/share/fonts/
sudo fc-cache -fv
```
